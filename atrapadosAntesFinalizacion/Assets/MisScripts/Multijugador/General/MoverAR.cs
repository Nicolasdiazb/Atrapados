using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
/*
public class MoverAR : MonoBehaviour
{
    private GameObject repJugadorAR;
    public Vector3 pos;
    private PhotonView PV;
    void Start()
    {
        //Represaentación de VR en AR
        repJugadorAR = GameObject.FindWithTag("cuboVR");
        pos = MoverJugadores.MJ.pos;
        PV = GetComponent<PhotonView>();
        
    }

    // Update is called once per frame
    void Update()
    {
        if(PV.IsMine)
        {
            repJugadorAR.transform.position = repJugadorAR.transform.TransformPoint(pos);
        }

    }

}*/
